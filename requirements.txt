# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.0
python-dotenv>=1.0
PyYAML>=6.0

# Telegram
python-telegram-bot[webhooks]>=21.0
# Voice-call stack — pinned as a set. py-tgcalls >= 2.3 requires a pyrogram
# fork with InputGroupCallSlug (pyrofork); stock pyrogram 2.0.106 lacks it.
# bootstrap.sh patches the remaining Groupcall* error imports post-install.
# Revisit when migrating to pyrofork (v2.1 candidate).
pyrogram==2.0.106
tgcrypto
py-tgcalls==2.2.11
ntgcalls==2.1.0

# AI / Claude
# claude-code CLI is installed via npm/native installer, not pip
claude-agent-sdk>=0.2.110              # Brain backend (src/brains/claude_sdk.py)

# Voice — Speech-to-Text
# faster-whisper                   # preferred STT backend
openai-whisper                   # fallback STT backend; install with: pip install openai-whisper --no-deps

# Voice — Text-to-Speech
edge-tts

# Browser
playwright                       # after install run: playwright install chromium

# Google Workspace
google-api-python-client
google-auth-oauthlib
google-auth-httplib2

# GitHub
PyGithub

# Monitoring & Scheduling
APScheduler>=3.10.0
psutil>=5.9.0

# Utilities
ddgs                             # DuckDuckGo search (src/tools/web_search.py)
beautifulsoup4                   # HTML parsing (src/tools/link_reader.py)
Pillow                           # image processing (src/tools/vision.py)

# ML (CPU only)
# Install separately to avoid pulling 3GB of NVIDIA drivers:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
python-multipart

# Dev / tests
pytest
